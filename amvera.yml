meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: src/bot/main.py
  command: python src/bot/main.py
  persistenceMount: /data
  containerPort: "80"
