meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: app.py
  command: python -m src.bot.main
  persistenceMount: /data
  containerPort: "80"
